# Organizer photos

Add headshot photos here for the Team page. Suggested filename pattern: firstname-lastname.jpg (e.g. `ahmed-bebars.jpg`).

**Recommended:** 400×400px, square crop, JPG or PNG.

After adding a file, update `src/pages/team.js`: either import the image and set `image` to that import, or use Gatsby's `StaticImage` from `gatsby-plugin-image`.
